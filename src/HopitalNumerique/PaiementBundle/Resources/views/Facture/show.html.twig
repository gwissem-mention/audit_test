{% extends 'NodevoAdminBundle::admin.html.twig' %}

{% block h1Title %}
    Fiche Facture
{% endblock %}

{% block toolbarButton %}
    <a class="btn btn-default" href="{{ path('hopitalnumerique_paiement_facture') }}" title="Retour à la liste"><i class="fa fa-reply"></i></a>
    {% if facture.id %}
        <a href="{{ path('hopitalnumerique_paiement_facture_edit', { 'id': facture.id }) }}" class="btn btn-default" title="Editer"><i class="fa fa-edit"></i></a>
    {% endif %}
{% endblock %}

{% block body %}
    <div class="col-sm-12">
        <div class="panel panel-midnightblue">
            <div class="panel-heading">
                <h4>Informations</h4>
            </div>

            <div class="panel-body" >
                <p>
                    <strong>Id : </strong>
                    {{ facture.id }}
                </p>
                <p>
                    <strong>Datecreation : </strong>
                    {{ facture.dateCreation }}
                </p>
            </div>
        </div>
    </div>
{% endblock %}
