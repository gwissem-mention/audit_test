// nécessite un lien (id getrechercheTexte) avec pour href, l'url du formulaire
tinymce.PluginManager.add('rechercheTexte', function(editor, url) {

    // Add a button that opens a window
    editor.addButton('rechercheTexte', {
        text: 'Recherche texte',
        icon: false,
        onclick: function() {
        	editor.insertContent('[RECHERCHETEXTE:0;;]');
        }
    });
});