tinymce.PluginManager.add('infradoc', function(editor, url) {
    // Add a button that opens a window
    editor.addButton('infradoc', {
        text: 'Ajouter un lien vers un infradoc',
        icon: false,
        onclick: function() {
            // Open window
            editor.windowManager.open({
                title: 'Entrer l\'ID de l\'infradoc',
                body: [
                    {type: 'textbox', name: 'id', label: 'ID'}
                ],
                onsubmit: function(e) {
                    // Insert content when the window form is submitted
                    editor.insertContent('[INFRADOC:' + e.data.id + ']');
                }
            });
        }
    });
});