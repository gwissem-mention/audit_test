{% extends 'NodevoAdminBundle::form.html.twig' %}

{% block form_row %}
    {% spaceless %}
        <div class="form-group">
            <div class="col-md-12">
                {{ form_widget(form) }}
            </div>
            <div class="col-md-12">
                <div class="help-block">
                    {{ form_errors(form) }}
                </div>
            </div>
        </div>
    {% endspaceless %}
{% endblock form_row %}