<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Container</th>
            <th>Réponse</th>
            <th>Plan d'action</th>
            <th>Lien</th>
        </tr>
    </thead>
    <tbody>
        {% for data in result.items %}
            <tr class="root">
                <td class="container-label">{{ data.label }}</td>
                <td></td>
                <td>
                    {% if data.actionPlan is not null %}
                        {{ data.actionPlan.description }}
                    {% endif %}
                </td>
            </tr>
            {% for children in data.childrens %}
                <tr>
                    <td class="container-label">{{ children.label }}</td>
                    <td></td>
                    <td>
                        {% if children.actionPlan is not null %}
                            {{ children.actionPlan.description }}
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        {% endfor %}
    </tbody>
</table>


