<form action="{{ path('hopitalnumerique_autodiag_entry_attribute_save', {"entry": entry.id, "attribute": attribute.id}) }}">

    <div class="attribute-value">
        <div class="widget">
            {% block form_content %}{% endblock %}
        </div>

        <div class="tools">
            <div class="attribute-mood"></div>
            <i title="{{ 'attribute.comment.add'|trans }}" class="comment-button fa fa-comment tool-comment"></i>
        </div>

        <div class="clearfix"></div>
    </div>

    <div class="attribute-comment">
        <textarea name="value[comment]"></textarea>
    </div>

</form>
