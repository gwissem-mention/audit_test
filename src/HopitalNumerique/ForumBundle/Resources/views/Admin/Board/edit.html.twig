{% extends 'CCDNForumForumBundle:Common:Layout/base.html.twig' %}

{% set LayoutTemplate = ccdn_forum_forum.board.admin.edit.layout_template %}

{%- block title -%}
    {{- 'title.admin.manage-boards.edit' |trans({'%board_name%': board.name}, 'CCDNForumForumBundle') -}}
{%- endblock title -%}

{%- block sidebar -%}
    {%- spaceless -%}
    {{- parent() -}}
    {%- include 'CCDNForumForumBundle:Common:Layout/Sidebar/admin_forum.html.twig' -%}
    {%- endspaceless -%}
{%- endblock sidebar -%}

{% block information_page %}
    <div class="col-md-12">
        <h3 class="violet">Forum - Administration </h3>
    </div>
{% endblock information_page %}

{%- block body_content -%}
    {%- spaceless -%}

    <section class="clearfix">
        {%- form_theme form ccdn_forum_forum.board.admin.edit.form_theme -%}
        <form action="{{- path('ccdn_forum_admin_board_edit_process', {'boardId': board.id, 'forum_filter': forum_filter, 'category_filter': category_filter}) -}}"
         method="post" {{ form_enctype(form) }} class="form-horizontal toValidate" novalidate>
            <fieldset>

                <div class="form-group">
                    <div id="titre-creation">
                        <h4>
                            {{- 'title.admin.manage-boards.edit' |trans({'%board_name%': board.name}, 'CCDNForumForumBundle') -}}
                        </h4>
                    </div>
                </div>

                {{- form_errors(form) -}}

                {{- form_row(form.category) -}}

                {{- form_row(form.name) -}}

                {{- form_row(form.description) -}}
                
                <div class="row">
                    <button type="button" class="btn btn-link col-lg-offset-2 col-lg-2" onclick="selectionnerAllRolesReadEditBoard();">
                        <span class="fa fa-check-square-o"></span> Sélectionner tout
                    </button>
                    <button type="button" class="btn btn-link col-lg-offset-1 col-lg-3" onclick="deselectionnerAllRolesReadEditBoard();">
                        <span class="fa fa-square-o"></span> Désélectionner tout
                    </button>
                </div>

                {{form_row(form.readAuthorisedRoles)}}
                
                <div class="row">
                    <button type="button" class="btn btn-link col-lg-offset-2 col-lg-2" onclick="selectionnerAllRolesNewEditBoard();">
                        <span class="fa fa-check-square-o"></span> Sélectionner tout
                    </button>
                    <button type="button" class="btn btn-link col-lg-offset-1 col-lg-3" onclick="deselectionnerAllRolesNewEditBoard();">
                        <span class="fa fa-square-o"></span> Désélectionner tout
                    </button>
                </div>

                {{form_row(form.topicCreateAuthorisedRoles)}}
                
                <div class="row">
                    <button type="button" class="btn btn-link col-lg-offset-2 col-lg-2" onclick="selectionnerAllRolesReplyEditBoard();">
                        <span class="fa fa-check-square-o"></span> Sélectionner tout
                    </button>
                    <button type="button" class="btn btn-link col-lg-offset-1 col-lg-3" onclick="deselectionnerAllRolesReplyEditBoard();">
                        <span class="fa fa-square-o"></span> Désélectionner tout
                    </button>
                </div>

                {{form_row(form.topicReplyAuthorisedRoles)}}

                {{- form_rest(form) -}}

                <div class="row">
                    <input type="submit" class="btn btn-success col-lg-offset-2 col-lg-3" value="
                        {{- 'form.button.save-changes' |trans({}, ForumBundle.translation ) -}}
                    " name="submit[post]" id="submit[post]">

                    <a class="btn btn-default col-lg-offset-1 col-lg-2" href="{{- path('ccdn_forum_admin_board_list', {'forum_filter': forum_filter, 'category_filter': category_filter}) -}}">
                        {{- 'form.button.cancel' |trans({}, ForumBundle.translation ) -}}
                    </a>
                </div>

            </fieldset>
        </form>
    </section>

    {%- endspaceless -%}
{%- endblock body_content -%}
