{%- spaceless -%}

    {% if post.enAttente %}

        <div class="label label-danger center-block">Les liens de ce post sont en attente de modération de la part d'un administrateur.</div>

        {%- if is_granted('ROLE_ADMINISTRATEUR_1') -%}

                <div class="bg-danger">{{- BBCode_Parse(post.getBody)|raw -}}</div>

                <div class="clearfix"></div>
                <a class="pull-right btn btn-primary" style="margin-top:10px;" href="{{path('ccdn_forum_user_post_activation_enattente', {'post': post.id})}}"><i class="fa fa-check"></i> Valider le lien</a>

        {% endif %}

    {% else %}

        {{- BBCode_Parse(post.getBody)|raw -}}

    {% endif %}

{%- endspaceless -%}