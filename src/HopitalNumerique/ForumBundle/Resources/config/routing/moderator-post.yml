
ccdn_forum_moderator_post_lock:
    pattern: /{postId}/lock
    defaults: { _controller: HopitalNumeriqueForumBundle:ModeratorPost:lock, _locale: fr }

ccdn_forum_moderator_post_unlock:
    pattern: /{postId}/unlock
    defaults: { _controller: HopitalNumeriqueForumBundle:ModeratorPost:unlock, _locale: fr }

ccdn_forum_moderator_post_unlock_process:
    pattern: /{postId}/unlock/process
    defaults: { _controller: HopitalNumeriqueForumBundle:ModeratorPost:unlockProcess, _locale: fr }

ccdn_forum_moderator_post_restore:
    pattern: /{postId}/restore
    defaults: { _controller: HopitalNumeriqueForumBundle:ModeratorPost:restore, _locale: fr }
